node_modules/
dist/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*
/.figma/design-context/

# Archives & Bundles (prevents hitting GitHub 100MB limit)
*.zip
*.tar.gz
*.tgz

# Android builds and caches
android/.gradle/
android/app/build/
android/.idea/
android/local.properties
.gradle/

# OS files
.DS_Store
Thumbs.db

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/

